#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>

struct employee {
	char Surname[100];
	char Post[100];
	int Salary;
	int DateOfSigning;
	int ValidityPeriod;
};

typedef struct employee Employee;

void main() {
	Employee Ivanov, Pupkin, Jobs;

	Ivanov = (Employee){ "Ivanov", "Brigadier", 30000, 2018, 13};
	Pupkin = (Employee){ "Pupkin", "Loader", 20000, 2019, 27};
	Jobs = (Employee){ "Jobs", "Guard", 15000, 2021, 4 };
	//printf("%s\n", Ivanov.Surname);

	Employee SN[3];
	SN[0] = Ivanov;
	SN[1] = Pupkin;
	SN[2] = Jobs;

	for (int i = 0; i < 3; i++) {
		//printf("%s\n", SN[i].Surname);
		if (SN[i].ValidityPeriod < 5 || SN[i].DateOfSigning == 2021) {
			printf("%s\n", SN[i].Surname);
		}
		
	}
}
